<h1>Posts</h1>
<% @posts.each do |post| %>
  <div class="well">

	<h3><%= post.title  %></h3>
	<h3><%= post.release  %></h3>
	<%= link_to "More...", mrelease_path(post), :class => 'btn btn-default' %>

  </div>
<% end %>
